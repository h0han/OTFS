#include <stdio.h>
#include <stlib.h>


int checkb(int fd, int iorb){
	if (i == 0){ //for inode bitmap
		lseek(fd,1024, SEEK_SET);
		for(int i = 0; i<11	
	}
	else if (i == 1){ //for data bitmap
	}
	else{
		printf("Parameter should be 0 or 1!");
	}

}

