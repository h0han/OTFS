#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>

int checkb(int iorb){
    int fd;
    unsigned char* buf;
    unsigned char comp;
    int count;
    int byte;
}

void chanb(int num, int iorb){
    int fd;
    unsigned char* buf;
    unsigned char com;
    unsigned char new;
    int byte;
}
